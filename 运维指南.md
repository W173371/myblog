# 博客运维指南

> 本指南覆盖日常开发、Git/GitHub 推拉、部署、故障排查等所有运维场景。

---

## 目录

1. [环境准备](#1-环境准备)
2. [日常写作一条龙](#2-日常写作一条龙)
3. [Git 与 GitHub 工作流](#3-git-与-github-工作流)
4. [部署上线](#4-部署上线)
5. [项目备份与恢复](#5-项目备份与恢复)
6. [主题更新](#6-主题更新)
7. [图片与静态资源管理](#7-图片与静态资源管理)
8. [SEO 与网站分析](#8-seo-与网站分析)
9. [多设备协作](#9-多设备协作)
10. [故障排查](#10-故障排查)
11. [常用命令速查](#11-常用命令速查)

---

## 1. 环境准备

### 1.1 所需软件

| 软件 | 版本要求 | 检查命令 |
|------|---------|----------|
| Hugo | ≥ 0.158.0 **Extended 版** | `hugo version` |
| Git | 任意 | `git --version` |
| Dart Sass | 已内置在 `bin/dart-sass/` | `bin/dart-sass/dart-sass/sass.bat --version` |

> ⚠️ **必须用 Hugo Extended 版**，否则编译 SCSS 会报错。执行 `hugo version` 应看到 `extended` 字样。

### 1.2 首次初始化 Git（⚠️ 当前还未做）

```powershell
# 进入项目根目录
cd e:\hugo\blog\myblog

# 初始化 Git 仓库
git init

# 添加所有文件（.gitignore 已配好，会自动排除 public/、resources/）
git add .

# 首次提交
git commit -m "🎉 初始化博客项目"
```

### 1.3 连接 GitHub

```powershell
# 在 GitHub 上创建仓库后（例如 weijunhao/myblog），执行：
git remote add origin https://github.com/weijunhao/myblog.git
git branch -M main
git push -u origin main
```

> 之后可以修改 `hugo.toml` 中的 `enableGitInfo = true`，页面底部会显示"最后修改时间"。

---

## 2. 日常写作一条龙

### 2.1 启动本地预览

```powershell
# 进入项目目录
cd e:\hugo\blog\myblog

# ⚠️ 必须设置 Dart Sass 环境变量
$env:PATH = "e:\hugo\blog\myblog\bin\dart-sass;$env:PATH"

# 启动预览服务器（含草稿文章）
hugo server -D

# 浏览器访问 http://localhost:1313
```

| 参数 | 作用 |
|------|------|
| `-D` | 包含草稿文章 |
| `--navigateToChanged` | 修改文件后自动刷新浏览器 |
| `--disableFastRender` | 全量重建（有时改配置需要） |
| `-p 8080` | 改为 8080 端口 |

> 💡 **快捷方式**：可以把上面三行存成 `start.ps1` 脚本，双击即启动。

### 2.2 创建新文章

```powershell
hugo new posts/文章标题.md
```

这会自动使用 `archetypes/posts.md` 模板，生成完整的 Front Matter。然后：

1. 修改 `title`、`date`、`categories`、`tags`
2. 把 `draft` 改为 `true`（写作中）或 `false`（写完发布）
3. 删除不需要的字段或保持为空
4. 在 `<!--more-->` 下面开始写正文

### 2.3 写文章

```
vim content/posts/文章标题.md    # 或用 VS Code / Typora
```

- 截图 / 配图放 `static/images/posts/文章名/` 或在文章同级目录
- 图片引用用相对路径或 `/images/xxx.png`
- 详情见 `图片使用说明.md`

### 2.4 发布文章（把 draft 改 false）

```yaml
draft: false    # 改为 false 即发布
```

### 2.5 修改现有文章

直接编辑 `content/posts/xxx.md`，保存后 `hugo server` 会自动刷新。

### 2.6 删除文章

```powershell
Remove-Item content/posts/xxx.md
# 或
git rm content/posts/xxx.md
```

---

## 3. Git 与 GitHub 工作流

### 3.1 推荐日常工作流

```powershell
# 1. 开始工作前拉取最新
git pull

# 2. 写文章 / 改配置...

# 3. 查看改了哪些文件
git status
git diff

# 4. 添加到暂存区
git add .
# 或只加特定文件：
git add content/posts/新文章.md hugo.toml

# 5. 提交
git commit -m "📝 新增文章：xxx"

# 6. 推送到 GitHub
git push
```

### 3.2 提交信息约定（建议）

| 前缀 | 含义 | 示例 |
|------|------|------|
| `📝` | 新增/修改文章 | `📝 新增：从0到1搭建博客` |
| `🎨` | 调整样式/模板 | `🎨 修改配色` |
| `⚙️` | 修改配置 | `⚙️ 更新 baseURL` |
| `🖼️` | 图片资源 | `🖼️ 添加文章配图` |
| `📦` | 主题更新 | `📦 升级 FixIt 到 v0.3.x` |
| `🐛` | 修 Bug | `🐛 修复友链页 404` |
| `📖` | 文档 | `📖 更新运维指南` |

### 3.3 查看日志

```powershell
git log --oneline --graph       # 简洁查看
git log -p hugo.toml            # 查看某个文件的历史
git show HEAD                   # 查看最新提交详情
```

### 3.4 回滚操作

```powershell
# 撤销工作区修改（还没 add 的）
git checkout -- 文件名

# 撤销暂存区（已经 add 还没 commit）
git reset HEAD 文件名

# 回退到上一个 commit（保留修改）
git reset HEAD~1

# 硬回退（丢弃修改！谨慎）
git reset --hard HEAD~1

# 回退单个文件到某个版本
git checkout HEAD~1 -- 文件路径
```

### 3.5 分支策略（建议）

```powershell
# 日常在 main 分支开发即可（个人博客不需要复杂分支）
git branch            # 查看当前分支
git checkout -b dev   # 创建并切换到 dev 分支（如果要尝试改大东西）
git checkout main     # 切回 main
git merge dev         # 合并 dev 到 main
git branch -d dev     # 删除 dev 分支
```

### 3.6 .gitignore 说明（已配置）

```
/public/              # 构建产物，不需要版本管理
/resources/           # Hugo 缓存
.hugo_build.lock      # 构建锁文件
.DS_Store             # macOS 系统文件
Thumbs.db             # Windows 系统文件
```

---

## 4. 部署上线

### 4.1 构建站点

```powershell
# ⚠️ 先设置 Dart Sass 环境变量
$env:PATH = "e:\hugo\blog\myblog\bin\dart-sass;$env:PATH"

# 构建（会把静态网站生成到 public/ 目录）
hugo

# 构建时包含草稿（一般部署时不加 -D）
hugo -D
```

### 4.2 部署前检查清单

- [ ] `hugo.toml` 中 `baseURL` 是否为正式域名（当前是占位的 https://example.com/）
- [ ] 文章 `draft: false` 确认
- [ ] Hugo 版本是否兼容（≥ 0.158.0 Extended）
- [ ] 本地 `hugo server -D` 无报错
- [ ] 浏览几个关键页面确认正常

### 4.3 部署方式

#### 方式一：GitHub Pages（推荐、免费）

```powershell
# 1. GitHub 仓库名设为 weijunhao.github.io
# 2. 在 GitHub 仓库 → Settings → Pages → Source 选 GitHub Actions
```

创建 `.github/workflows/hugo.yaml`：

```yaml
name: Deploy Hugo to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

defaults:
  run:
    shell: bash

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      HUGO_VERSION: 0.158.0
    steps:
      - uses: actions/checkout@v4

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v3
        with:
          hugo-version: ${{ env.HUGO_VERSION }}
          extended: true

      - name: Build
        run: hugo --minify

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./public

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

提交并推送后，每次 `git push` 到 main 分支都会自动部署。

#### 方式二：Vercel（推荐、免费、速度快）

1. 注册 [vercel.com](https://vercel.com)，用 GitHub 账号登录
2. 导入 GitHub 仓库
3. 框架选 **Hugo**，输出目录 `public`
4. 自定义域名在 Settings → Domains 设置

#### 方式三：Netlify（免费）

1. 注册 [netlify.com](https://netlify.com)
2. 导入 GitHub 仓库
3. Build command: `hugo --minify`
4. Publish directory: `public`

#### 方式四：Cloudflare Pages（免费、国内访问快）

1. 注册 [dash.cloudflare.com](https://dash.cloudflare.com)
2. Pages → 连接到 Git → 选择仓库
3. 生产分支：`main`
4. 构建命令：`hugo --minify`
5. 输出目录：`public`
6. 环境变量：`HUGO_VERSION` = `0.158.0`

### 4.4 自定义域名

部署后在平台设置里绑定域名，DNS 添加一条 CNAME 或 A 记录。然后修改 `hugo.toml`：

```toml
baseURL = "https://你的域名.com/"
```

### 4.5 部署小提示

- **每次推送前先本地构建一次**：`hugo` 看有没有错误
- 先部署再挂域名，不要颠倒
- GitHub Pages 默认域名 `weijunhao.github.io`，国内访问可能稍慢，可以加 CDN
- 修改 `hugo.toml` 后建议删除 `public/` 重建：`Remove-Item -Recurse public; hugo`

---

## 5. 项目备份与恢复

### 5.1 备份哪些文件

| 需要备份 | 不需要备份 |
|---------|-----------|
| `content/` （文章） | `public/` （构建产物） |
| `hugo.toml` （配置） | `resources/` （缓存） |
| `static/images/` （图片） | `.hugo_build.lock` |
| `layouts/` （自定义模板） | `bin/ds.zip`（Dart Sass 压缩包可重新下载） |
| `assets/` （自定义资源） |
| `archetypes/` （模板） |
| `data/` （数据文件） |
| `i18n/` （语言文件） |

### 5.2 备份方式

**方式一：GitHub 就是最好的备份**（推荐）

每次 `git push` 后，GitHub 上都有完整副本。只要确保 `static/images/` 的图片也提交了。

**方式二：压缩备份**

```powershell
# 排除 public/ 和 resources/
Compress-Archive -Path content,static,layouts,assets,archetypes,data,i18n,hugo.toml,archetypes,.gitignore -DestinationPath "blog-backup-$(Get-Date -Format yyyyMMdd).zip"
```

### 5.3 在新电脑恢复

```powershell
# 1. 安装 Hugo Extended 和 Git
# 2. 下载 Dart Sass 放到 bin/dart-sass/
# 3. 克隆仓库
git clone https://github.com/weijunhao/myblog.git
cd myblog

# 4. 下载主题（当前 themes/FixIt/ 已在仓库中则无需此步）
git submodule update --init --recursive

# 5. 启动测试
$env:PATH = "e:\hugo\blog\myblog\bin\dart-sass;$env:PATH"
hugo server -D
```

---

## 6. 主题更新

### 6.1 当前主题情况

当前主题 `FixIt` 直接在 `themes/FixIt/` 目录中（非 Git Submodule 方式），版本信息需进入主题目录查看。

### 6.2 更新方法

```powershell
# 方式一：手动替换（当前采用）
# 从 FixIt GitHub 下载最新 Release，覆盖 themes/FixIt/

# 方式二：改为 Git Submodule（推荐）
git submodule add https://github.com/hugo-fixit/FixIt.git themes/FixIt

# 之后更新主题只需
git submodule update --remote themes/FixIt
```

### 6.3 更新后验证

```powershell
$env:PATH = "e:\hugo\blog\myblog\bin\dart-sass;$env:PATH"
hugo server -D
# 浏览所有页面，特别是自定义过的部分
```

### 6.4 更新前建议

1. 先 `git commit` 当前所有修改
2. 备份 `themes/FixIt/` 目录
3. 记录当前主题版本号
4. 更新后如果出问题，`git checkout themes/FixIt/` 回滚

---

## 7. 图片与静态资源管理

### 7.1 图片放哪里

| 图片类型 | 存放路径 | 引用方式 |
|---------|---------|---------|
| 头像/Logo/通用图 | `static/images/xxx.png` | `/images/xxx.png` |
| 文章配图 | `static/images/posts/文章名/` | `/images/posts/文章名/xxx.png` |
| 文章内图（同级目录） | `content/posts/文章目录/` | `xxx.png`（相对路径） |

### 7.2 图片优化建议

```powershell
# 压缩 PNG（用 pngquant 或在线工具）
# 转 WebP 格式（更小）
# 大图 < 2MB，缩略图 < 200KB
```

参考 `图片使用说明.md` 了解更多细节。

### 7.3 静态文件注意事项

- 所有放 `static/` 下的文件会原样复制到 `public/` 根目录
- 不要与 `content/` 中的 Markdown 文件名冲突
- 大文件（视频等）不建议放 `static/`，用外部图床或 CDN

---

## 8. SEO 与网站分析

### 8.1 SEO 基础配置（hugo.toml）

```toml
# 已配置项：
enableRobotsTXT = true      # 自动生成 robots.txt（已开启）
hasCJKLanguage = true       # 中日韩语言优化（已开启）
googleAnalytics = ""        # GA 测量 ID（部署后填入）
```

### 8.2 添加 Google Analytics

```toml
# hugo.toml
[params.analytics]
  google = "G-XXXXXXXXXX"   # Google Analytics 4 测量 ID
```

### 8.3 添加百度统计

```toml
[params.analytics]
  baidu = "你的百度统计ID"
```

### 8.4 提交到搜索引擎

**Google Search Console**：
1. 访问 [search.google.com/search-console](https://search.google.com/search-console)
2. 添加网站属性（用域名验证）
3. 提交 `sitemap.xml`

**百度站长平台**（如果面向国内）：
1. 访问 [ziyuan.baidu.com](https://ziyuan.baidu.com)
2. 添加并验证网站
3. 提交 sitemap

### 8.5 友链与社交媒体

```toml
# hugo.toml 中配置：
[params.social]
  github = "你的GitHub用户名"
  # twitter, zhihu, weibo...
```

---

## 9. 多设备协作

### 9.1 场景：家里台式机 + 公司笔记本

```powershell
# 台式机（写完一篇文章后）
git add .
git commit -m "📝 新增文章：xxx"
git push

# 笔记本（到家继续写）
git pull
$env:PATH = "笔记本上的dart-sass路径;$env:PATH"
hugo server -D
```

### 9.2 注意事项

| 注意 | 说明 |
|------|------|
| **一定先 pull 再写** | 避免冲突，养成习惯 |
| **写完后立即 push** | 不要攒多篇文章一起提交 |
| **图片路径** | 用相对路径或 `/images/` 开头的绝对路径，保证两台机器一致 |
| **Dart Sass 路径** | 每台机器 `bin/dart-sass/` 位置不同，启动命令里改路径 |
| **baseURL** | 本地开发时无所谓，部署前统一检查 |

---

## 10. 故障排查

### 10.1 `hugo server` 报错：TOCSS: failed to transform SCSS

```
原因：Dart Sass 未安装或 PATH 未设置
解决：$env:PATH = "e:\hugo\blog\myblog\bin\dart-sass;$env:PATH"
```

### 10.2 `hugo version` 不显示 extended

```
原因：装了普通版 Hugo，不是 Extended 版
解决：从 github.com/gohugoio/hugo/releases 下载带 extended 的版本
文件名通常类似 hugo_extended_0.xxx.0_windows-amd64.zip
```

### 10.3 文章在本地能看到，部署后看不到

```
原因：draft: true（草稿状态）
解决：把文章的 draft 改为 false
```

### 10.4 修改配置后不生效

```
原因：hugo server 有缓存
解决：
1. Ctrl+C 停掉服务器
2. 删除 resources/ 目录
3. 重新启动 hugo server -D --disableFastRender
```

### 10.5 首页没有显示某篇文章

```
原因一：draft: true
原因二：date 是未来时间（晚于当前时间）
原因三：hiddenFromHomePage: true
```

### 10.6 图片不显示

```
原因一：路径写错（检查相对于 static/ 或文章的路径）
原因二：文件没有放到正确位置
原因三：文件名为大写但引用用了小写（Windows 不区分，Linux 部署后会区分）
解决：所有文件名用小写，用连字符不用下划线
```

### 10.7 `git push` 被拒绝

```
原因：远程仓库有新的提交，本地不是最新的
解决：
git pull --rebase
# 如果有冲突，解决冲突后：
git add .
git rebase --continue
git push
```

### 10.8 search.json 太大导致部署慢

```
原因：搜索索引包含了所有文章全文
解决：在 hugo.toml 中限制搜索内容长度：
[params.search]
  maxResultLength = 200
```

### 10.9 Git 仓库体积很大

```
原因：图片文件过大或提交了 public/ 目录
解决：
# 查看大文件
git rev-list --objects --all | git cat-file --batch-check='%(objecttype) %(objectname) %(objectsize) %(rest)' | awk '/^blob/ {print $3, $4}' | sort -nr | head -20

# 压缩大图或用 Git LFS 管理二进制文件
```

---

## 11. 常用命令速查

### 日常开发

```powershell
# 设置环境变量（每次新开终端都要做）
$env:PATH = "e:\hugo\blog\myblog\bin\dart-sass;$env:PATH"

# 启动本地预览（含草稿）
hugo server -D

# 启动本地预览（含草稿、自动刷新、全量重建）
hugo server -D --navigateToChanged --disableFastRender

# 新建文章
hugo new posts/文章标题.md

# 构建网站
hugo                    # 正式版（不含草稿）
hugo -D                 # 含草稿
hugo --minify           # 压缩版（部署用）
hugo --gc               # 构建后清理缓存
```

### Git 操作

```powershell
# 状态查看
git status                       # 工作区状态
git log --oneline -10            # 最近 10 条提交

# 提交流程
git pull                         # 拉取远程更新
git add .                        # 暂存所有修改
git commit -m "📝 xxx"           # 提交
git push                         # 推送到远程

# 分支操作
git branch -a                    # 查看所有分支
git checkout -b 新分支名         # 创建新分支
git merge 分支名                  # 合并分支

# 撤销
git checkout -- 文件名            # 撤销工作区修改
git reset HEAD 文件名             # 取消暂存
git stash                        # 暂存当前修改
git stash pop                    # 恢复暂存的修改
```

### 部署

```powershell
# 先本地构建验证
$env:PATH = "e:\hugo\blog\myblog\bin\dart-sass;$env:PATH"
hugo --minify

# 然后 git push，CI/CD 自动部署
git push
```

### 创建一个快捷脚本 `start.ps1`

在项目根目录创建：

```powershell
# start.ps1
$env:PATH = "e:\hugo\blog\myblog\bin\dart-sass;$env:PATH"
hugo server -D --navigateToChanged
```

之后双击或执行 `.\start.ps1` 即可启动。

---

## 附：一键初始化脚本（新电脑）

```powershell
# 克隆项目
git clone https://github.com/weijunhao/myblog.git
cd myblog

# 下载 Hugo Extended（从 GitHub Release 下载对应版本）
# 下载 Dart Sass（从 github.com/sass/dart-sass/releases 下载）
# 解压到 bin/dart-sass/

# 下载主题子模块（如果用了 submodule）
git submodule update --init --recursive

# 启动
$env:PATH = "e:\hugo\blog\myblog\bin\dart-sass;$env:PATH"
hugo server -D
```

---

> 📅 最后更新：2026-05-27
>
> 💡 有遗漏的场景？随时补充。
