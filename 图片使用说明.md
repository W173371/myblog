# FixIt 主题 — 图片使用完全指南

本文档整理了 FixIt 主题中**所有可以放图的位置**、每种图片的配置方式、尺寸建议和存放路径。

---

## 目录

1. [快速索引：一张表看懂全部图片位置](#快速索引)
2. [浏览器标签页图标（Favicon）](#1-浏览器标签页图标favicon)
3. [导航栏 Logo](#2-导航栏-logo)
4. [个人头像](#3-个人头像)
5. [文章封面图](#4-文章封面图)
6. [文章正文图片](#5-文章正文图片)
7. [SEO / 社交分享配图](#6-seo--社交分享配图)
8. [赞赏二维码](#7-赞赏二维码)
9. [Shortcode 增强图片](#8-shortcode-增强图片)
10. [图片灯箱](#9-图片灯箱)
11. [图片性能优化](#10-图片性能优化)

---

## 快速索引

| 位置 | 去哪配置 | 放在哪 | 推荐尺寸 | 当前状态 |
|------|----------|--------|----------|:--:|
| Favicon 图标 | 特定文件名，放 `static/` | `static/favicon.ico` | 32×32 / 16×16 | 可加 |
| SVG Favicon | `params.app.svgFavicon` | `static/` | 矢量 | 未设置 |
| 导航栏 Logo | `params.header.title.logo` | `static/images/` | 高度 40-60px | 未设置 |
| 作者头像（全局） | `params.author.avatar` | `static/images/` | 200×200 | ✅ `avatar.png` |
| 首页大头像 | `params.home.profile.avatarURL` | `static/images/` | 200×200 | ✅ `avatar.png` |
| Gravatar 头像 | `params.home.profile.gravatarEmail` | 邮箱绑定 | 自动 | 未设置 |
| 文章卡片预览图 | 文章 front matter `featuredImagePreview` | `static/images/` 或文章目录 | 16:9 比例 | 待添加 |
| 文章顶部大图 | 文章 front matter `featuredImage` | `static/images/` 或文章目录 | 宽 1200px+ | 待添加 |
| 文章内容插图 | Markdown `![](path)` | `static/images/` 或文章目录 | 任意 | ✅ cs.png |
| 全局 SEO 配图 | `params.seo.image` | `static/images/` | 1200×630 | 未设置 |
| 文章 SEO 配图 | 文章 front matter `images` | `static/images/` | 1200×630 | 未设置 |
| 微信赞赏码 | `params.page.reward.ways.wechatpay` | `static/images/` | 300×300 | 未设置 |
| 支付宝赞赏码 | `params.page.reward.ways.alipay` | `static/images/` | 300×300 | 未设置 |

---

## 1. 浏览器标签页图标（Favicon）

浏览器标签页左侧显示的小图标。

### 普通 Favicon（.ico / .png）

**无需配置**，只需将文件放在 `static/` 根目录：

```
static/
├── favicon.ico          # 通用图标（32×32，含 16×16、32×32 两种）
├── favicon-32x32.png    # 标准 favicon（32×32）
├── favicon-16x16.png    # 小尺寸 favicon（16×16）
├── apple-touch-icon.png # iOS 添加到桌面时的图标（180×180）
└── safari-pinned-tab.svg  # Safari 固定标签页 SVG 图标
```

> **推荐做法**：用 [realfavicongenerator.net](https://realfavicongenerator.net/) 一次生成全套。

### SVG Favicon

配置 `hugo.toml`：

```toml
[params.app]
  svgFavicon = "/favicon.svg"     # 放在 static/favicon.svg
  iconColor = "#5bbad5"           # Safari 标签页主题色
```

### 关闭 Favicon

```toml
[params.app]
  noFavicon = true    # 不加载任何 favicon 标签
```

---

## 2. 导航栏 Logo

替换导航栏左侧的纯文字标题。

### 配置

```toml
[params.header.title]
  logo = "/images/logo.png"    # 放在 static/images/logo.png
  name = "魏俊浩的博客"         # logo 的 alt 文字
```

### 说明

- 设置 `logo` 后，导航栏文字标题会被图片替换
- 图片路径相对于 `static/` 目录
- **推荐高度 40-60px**，宽度不限
- 支持 PNG / SVG / JPG

---

## 3. 个人头像

头像在**三处**显示，可以通过不同配置精细化控制。

### 3.1 作者头像（全局默认）

在文章底部作者信息区显示，也是其他位置的兜底头像。

```toml
[params.author]
  avatar = "/images/avatar.png"    # 放在 static/images/avatar.png
```

### 3.2 首页个人资料大图

首页顶部的个人资料区域，优先级最高。

```toml
[params.home.profile]
  avatarURL = "/images/avatar.png" # 放在 static/images/avatar.png
```

### 3.3 Gravatar 头像（邮箱绑定）

使用 Gravatar 服务自动匹配头像，无需本地文件。

```toml
[params.home.profile]
  gravatarEmail = "you@example.com"   # 注册 Gravatar 的邮箱
```

> **优先级**：`gravatarEmail` > `avatarURL` > `params.author.avatar`

### 3.4 单篇文章覆盖作者头像

在文章 front matter 中单独指定：

```toml
[author]
  avatar = "/images/special-avatar.png"
```

### 3.5 文章页面是否显示头像

```toml
[params.page]
  authorAvatar = true    # false = 全站不显示
```

### 推荐尺寸

- **正方形**，推荐 **200 × 200** 像素
- 当前 `avatar.png` 约 625 KB，**建议压缩到 50 KB 以内**（可用 TinyPNG）

---

## 4. 文章封面图

文章封面图分两种：列表页小图和文章页大图。

### 4.1 列表页预览图（featuredImagePreview）

显示在首页文章列表、分类列表、标签列表的文章卡片上。

**在文章 front matter 中设置**：

```toml
featuredImagePreview = "/images/cover.jpg"
```

**全局开关**（是否显示这些卡片图）：

```toml
[params.home.posts]
  imagePreview = true    # 当前已开启
```

### 4.2 文章顶部大图（featuredImage）

显示在文章正文标题上方，作为文章顶部的 Hero 大图。

**在文章 front matter 中设置**：

```toml
featuredImage = "/images/hero.jpg"    # 推荐宽 1200px+
```

### 4.3 图片存放路径

两种方式任选其一：

| 方式 | 路径示例 | 适用场景 |
|------|----------|----------|
| 全局静态目录 | `"/images/cover.jpg"` → `static/images/cover.jpg` | 通用配图，多篇文章共用 |
| 文章同级目录（Page Bundle） | `"cover.jpg"` → `content/posts/my-post/cover.jpg` | 文章专属配图 |

> **使用 Page Bundle 时**：新建文章用目录而非文件，图片和 .md 放一起：
> ```
> content/posts/my-post/
> ├── index.md          # 文章内容
> ├── cover.jpg         # 封面
> └── screenshot.png    # 文中插图
> ```

### 推荐尺寸

| 类型 | 推荐尺寸 | 比例 |
|------|----------|:--:|
| 列表预览图 | 800×450 | 16:9 |
| 顶部大图 | 1200×675 | 16:9 |
| 方形卡片 | 600×600 | 1:1 |

---

## 5. 文章正文图片

在 Markdown 正文中插入图片，最常用的方式。

### 基础语法

```markdown
![图片描述](/images/photo.jpg)
```

### 存放位置

| 方式 | 写法 | 存放位置 |
|------|------|----------|
| 静态目录 | `![](/images/photo.jpg)` | `static/images/photo.jpg` |
| 文章 Bundle | `![](photo.jpg)` | `content/posts/文章名/photo.jpg` |
| 外部链接 | `![](https://example.com/img.jpg)` | 互联网 URL |

### 带标题的图片（触发灯箱）

```markdown
![点击可放大查看](photo.jpg "图片标题")
```

**注意**：只有带 `title`（引号内的文字）的图片才会触发灯箱效果。

### 强制所有图片可点开放大

```toml
[params.page]
  lightgallery = "force"    # 改为 "force"
```

---

## 6. SEO / 社交分享配图

文章分享到微信、Twitter、Facebook 时显示的预览图。

### 6.1 全站默认配图

```toml
[params.seo]
  image = "/images/og-default.jpg"    # 1200×630 推荐
```

### 6.2 文章专属配图（优先级更高）

在文章 front matter 中设置：

```toml
images = ["/images/og-article.jpg"]
```

### 6.3 文章 SEO 发布者 Logo

```toml
[params.page.seo.publisher]
  logoUrl = "/images/logo.png"
```

### 推荐尺寸

- **OG 图片**：**1200 × 630** 像素（Facebook/Twitter 标准）
- 微信分享：300×300

---

## 7. 赞赏二维码

文章底部"赞赏/打赏"功能的收款二维码。

### 配置

```toml
[params.page.reward]
  enable = true    # 先打开总开关

  [params.page.reward.ways]
    wechatpay = "/images/wechatpay.png"    # 微信收款码
    alipay    = "/images/alipay.png"       # 支付宝收款码
    paypal    = "/images/paypal.png"       # PayPal
    bitcoin   = "/images/bitcoin.png"      # 比特币地址二维码
```

### 推荐尺寸

- **300 × 300** 像素，正方形
- PNG 格式，确保清晰

---

## 8. Shortcode 增强图片

FixIt 提供了图片相关 Shortcode，在 Markdown 中调用，比 `![]( )` 功能更强。

### 8.1 `image` Shortcode — 增强图片

支持设置标题、缩放、灯箱、宽度等。

```markdown
{{</* image src="/images/photo.jpg" alt="描述" caption="图片标题" width="600px" */>}}
```

**完整参数**：

| 参数 | 说明 | 示例 |
|------|------|------|
| `src` | 图片路径（必填） | `"/images/photo.jpg"` |
| `alt` | 替代文字 | `"架构图"` |
| `caption` | 图片下方标题 | `"图1：系统架构"` |
| `title` | 鼠标悬浮提示 | `"点击放大"` |
| `width` | 宽度 | `"600px"` 或 `"80%"` |
| `height` | 高度 | `"400px"` |
| `src_s` | 小图（懒加载用） | `"/images/photo_s.jpg"` |
| `src_l` | 大图（灯箱用） | `"/images/photo_l.jpg"` |
| `linked` | 是否可点击 | `true` / `false` |

### 8.2 `figure` Shortcode — 带说明的图片组

```markdown
{{</* figure src="/images/photo.jpg" title="示例图片" caption="这展示了系统的架构" */>}}
```

### 8.3 `admonition` 中的特殊图片

```markdown
{{</* admonition type="image" src="/images/screenshot.png" title="页面截图" */>}}
```

---

## 9. 图片灯箱

点击图片后弹出大图预览（暗色遮罩 + 放大）。

### 配置

```toml
[params.page]
  lightgallery = true
```

**三种模式**：

| 值 | 效果 |
|:--|------|
| `true` | 只有带 `title` 属性的图片可点开放大 |
| `"force"` | **所有**图片都可点开放大 |
| `false` | 完全禁用灯箱 |

### 触发条件

```markdown
<!-- 可触发灯箱 -->
![描述](photo.jpg "图片标题")

<!-- 不可触发灯箱（没有 title） -->
![描述](photo.jpg)
```

### 单篇文章覆盖

```toml
lightgallery = "force"    # 这篇文章强制所有图片可放大
```

---

## 10. 图片性能优化

### 10.1 Hugo 自动压缩

```toml
[imaging]
  quality = 75    # JPEG 质量，范围 1-100
```

### 10.2 远程图片缓存 + 格式转换

```toml
[params.image]
  cacheRemote = true     # 把远程图片下载到本地（避免外部链接失效）
  optimise = true        # 自动生成 WebP 格式 + 调整尺寸
  blackList = []         # 不对这些文件做优化，如 ["logo.png"]
```

> ⚠️ `optimise = true` 会显著增加构建时间，建议仅在发布前开启。

### 10.3 懒加载

FixIt 主题默认对所有 Markdown 图片启用懒加载（`loading="lazy"`），无需额外配置。

---

## 附录：所有图片相关配置速查表

```toml
# ============ hugo.toml ============

# Hugo 全局压缩质量
[imaging]
  quality = 75

# SEO 默认配图
[params]
  images = ["/images/og-default.jpg"]

# 作者头像
[params.author]
  avatar = "/images/avatar.png"

# App 图标
[params.app]
  svgFavicon = "/favicon.svg"
  iconColor = "#5bbad5"

# 导航栏 Logo
[params.header.title]
  logo = "/images/logo.png"

# 首页大头像
[params.home.profile]
  avatarURL = "/images/avatar.png"
  gravatarEmail = "you@example.com"

# 显示文章卡片预览图
[params.home.posts]
  imagePreview = true

# 全局 SEO 图
[params.seo]
  image = "/images/og-default.jpg"

# 图片优化
[params.image]
  cacheRemote = true
  optimise = true

# 发布者 Logo
[params.page.seo.publisher]
  logoUrl = "/images/logo.png"

# 灯箱开关
[params.page]
  lightgallery = true
  authorAvatar = true

# 打赏码
[params.page.reward]
  enable = true
  [params.page.reward.ways]
    wechatpay = "/images/wechatpay.png"
    alipay    = "/images/alipay.png"
```

---

## 图片存放路径总结

| 存放方式 | 路径 | 引用写法 | 适合 |
|----------|------|----------|------|
| `static/` 目录 | `static/images/photo.jpg` | `/images/photo.jpg` | 全局共用（Logo、头像、SEO图） |
| 文章 Page Bundle | `content/posts/xxx/photo.jpg` | `photo.jpg` | 文章专属（封面、插图） |
| 远程 URL | 互联网 | `https://example.com/img.jpg` | 外部图床 |

---

> 最后更新：2026-05-27
